#include <stdio.h>
#include <string.h>

const char *PROVINCES[] = {"Alberta", "British Columbia", "Manitoba", "New Brunswick", "Newfoundland and Labrador", "Nova Scotia", "Ontario", "Prince Edward Island", "Quebec", "Saskatchewan"};

void void print_provinces()
{
    //TODO:: Write a for loop that iterates over the PROVINCES array and prints out how many letters is contained in each

    
    province_size = sizeof(PROVINCES)/sizeof(PROVINCES);

    for (int i = 0; i < province_size, i++){

    

    }
    
    






    string str = get_string("what string do you want to know the ascii alues of the letters of?");

    int strSize = strlen(str);

    int i = 0;

    while (i < strSize)
    {
        int asciiVal = str[i]
        printf("The char at position %d" is %c and the ASCII value is %d",i, str[i], str[i]);
        i++;

    }
    for()
}